use ascii to light up buttons
o
public class Node {
  int value;
  Node left, node;}
  GUI () {
      for (; i<26; ) 
        buttons.add(new JButton('a' + i));
        buttons[i].setSize //buttons.get(i)
        buttons[i].setLocation
        // float layout
        pane.add(buttons[i]);
}
// alternative: if you've alredy inited all buttons:
    // in constructor      buttons.add(a)
// ? create array of jbuttons?


/highlighting
keyPressed(e){
    buttons.get(e.getKeyCode() - 65).setBGcolor  //or eycahr?j
    (int)e.getKeyChar()  - 'a'
    //shift functionality - 16 in ascii
    //if key is shift...
